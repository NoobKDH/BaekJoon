#include <stdio.h>

int main(void){
    double a,b;
    double m;
 
    scanf("%lf %lf",&a,&b);
    m = a*((100-b)/100);
    if(m>=100)
        printf("0");
    else
        printf("1");
    return 0;
}
