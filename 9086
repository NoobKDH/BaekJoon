#include <stdio.h>
#include <string.h>
int main(void){
    int n,a;
    char c[1000];
    scanf("%d",&n);
    for(int i = 0; i<n;i++){
        scanf("%s", c);
        a = strlen(c);
        printf("%c%c\n",c[0], c[a-1]);
    }
    return 0;
}
