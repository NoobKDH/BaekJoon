#include <stdio.h>

int main(void){
    int x, n;
    int a[10000];
    scanf("%d %d", &x, &n);
    for(int i=0; i<x;i++){
        scanf("%d",&a[i]);
    }
    for(int i =0; i<x; i++){
        if(a[i]<n)
            printf("%d ",a[i]);
    }
    
    return 0;
}
