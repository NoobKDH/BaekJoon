#include <stdio.h>
#include <math.h>

int main(void){
    int D,H,W;
    scanf("%d %d %d", &D, &H, &W);
    double d;
    d = sqrt((H*H)+(W*W));
    double h1 = D*H/d;
    double w1 = D*W/d;
    printf("%d %d",(int)h1, (int)w1);
    
    return 0;
}
